# clantic

A Clojure library.

## Usage

```clojure
(require '[clantic.core :as clantic])

(clantic/hello "world")
;; => "Hello, world!"
```

## Development

```bash
make repl  # Start REPL
make test  # Run tests
```
